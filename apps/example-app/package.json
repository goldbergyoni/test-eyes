{
  "name": "example-app",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:ci": "vitest run --reporter=junit --outputFile=test-results.xml",
    "parse-junit": "tsx scripts/parse-junit.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fast-xml-parser": "^4.5.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.5"
  }
}
