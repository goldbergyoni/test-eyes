{
  "name": "tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=junit --outputFile=../junit.xml"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
